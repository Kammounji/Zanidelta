{% extends "MyAppUserBundle:Default:layout.html.twig" %}
{% form_theme form 'bootstrap_3_layout.html.twig' %}
{# {% form_theme form 'bootstrap_3_horizontal_layout.html.twig' %} #}
        {% block head %}
            {{ parent() }}
        {% endblock %}
    {% block content %}

        <div class="cp-wrapper">

            {% block header %}
                {{ parent() }}
            {% endblock %}

            <section class="cp-inner-banner">
                <div class="container">
                    <div class="cp-inner-banner-outer">
                        <h2>Espace Ajout Evennement</h2>

                        <ul class="breadcrumb">
                            <li><a href="{{ path("affiche") }}">Home</a></li>
                            <li>Espace Association</li>
                            <li class="active">EA-Evennement</li>
                        </ul>
                    </div>
                </div>
            </section>

<center>
{{ form(form) }}

            </center>
            {% block footer %}
                {{ parent() }}
            {% endblock %}
        </div>

        {% block script %}
            {{ parent() }}
        {% endblock %}
    {% endblock %}
